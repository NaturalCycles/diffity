{
  "name": "@diffity/server",
  "version": "0.10.38",
  "private": true,
  "type": "module",
  "description": "Hosted, multi-user diffity: the review UI and an OAuth-protected MCP endpoint",
  "bin": {
    "diffity-server": "./dist/index.js"
  },
  "scripts": {
    "build": "tsx build.ts",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.typecheck.json"
  },
  "engines": {
    "node": ">=22.13"
  },
  "devDependencies": {
    "@diffity/api": "*",
    "@diffity/git": "*",
    "@diffity/parser": "*",
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "express": "^5.2.1",
    "zod": "^4.6.5"
  }
}
